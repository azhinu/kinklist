# Kinklist

With this app you can create and share your own kinklists.

### Flags and envs

- `--path`, `-p` `KL_DB_PATH`: Path to the database file (default: `./kinklist.db`)
- `--port`, `-P` `KL_PORT`: Listening port (default: `8080`)

- `--version`, `-v`: Print version information and quit
